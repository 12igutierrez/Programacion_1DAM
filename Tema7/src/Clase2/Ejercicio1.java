package Clase2;

public class Ejercicio1 {
	/*Crea la clase PrismaPentagonal que contenga:
	 * - double altura
	 * - double apotema
	 * - double medida arista base
	 * - double area base
	 * - double area lateral
	 * - Un metodo que calcule el area de la base (perimetro de la base x apotema) y
	 *   lo guarde en la variable area_base
	 * - Un metodo que calcule el area lateral (perimetro de la base x altura) y lo guarde
	 *   en la variable area_lateral
	 * - Un metodo que calcule el area total del prisma y lo devuelva como double (area_base
	 *   x2 + area_lateral)
	 * - Un metodo que calcule el volumen y lo devuelva (area_base x altura)
	 */
	public static void main(String[] args) {

	}

}
